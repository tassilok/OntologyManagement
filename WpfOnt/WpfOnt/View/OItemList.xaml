<UserControl x:Class="WpfOnt.View.OItemList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:WpfOnt.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:converter="clr-namespace:WpfOnt.Converter"
             x:Name="ucOItemList">
    <UserControl.DataContext>
        <viewModel:OItemListModel/>
    </UserControl.DataContext>
    <Grid>
        <DataGrid ItemsSource="{Binding Path=ItemList}" DataContext="{Binding ItemList}">
            
        </DataGrid>
    
    </Grid>
</UserControl>
